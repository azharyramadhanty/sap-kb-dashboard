apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fe-cms.fullname" . }}-test-connection"
  labels:
    {{- include "fe-cms.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fe-cms.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
