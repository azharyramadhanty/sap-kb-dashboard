apiVersion: v1
kind: Service
metadata:
  name: {{ include "fe-cms.fullname" . }}
  labels:
    {{- include "fe-cms.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "fe-cms.selectorLabels" . | nindent 4 }}
